{{- $v := .Values -}}
{{- $global := $v.global -}}
{{- $main := $v.reposrv -}}
{{- $svc := $main.service -}}

--- # --------------------------------------------------------------------------------- NetworkPolicy
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "common.fullname" . }}-reposrv
  labels:
    app.kubernetes.io/component: reposrv
    {{- include "common.defaultLabels" . | nindent 4 }}
    {{- include "common.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $v.labels 
                          $main.labels ) ) | nindent 4 }}
  annotations:
    {{- include "common.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $v.annotations 
                          $main.annotations ) ) | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: reposrv
      {{- include "common.defaultSelectorLabels" . | nindent 4 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: server
              {{- include "common.defaultSelectorLabels" . | nindent 14 }}
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: applicationcontroller
              {{- include "common.defaultSelectorLabels" . | nindent 14 }}
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: notificationscontroller
              {{- include "common.defaultSelectorLabels" . | nindent 14 }}
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: applicationsetcontroller
              {{- include "common.defaultSelectorLabels" . | nindent 14 }}
      ports:
        - port: {{ $svc.ports.http.port }}
          protocol: TCP
    - from:
        - namespaceSelector: {}
      ports:
        - port: {{ $svc.ports.metrics.port }}