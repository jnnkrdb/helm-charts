{{- $v := .Values -}}
{{- $global := $v.global -}}
{{- $main := $v.applicationsetcontroller -}}
{{- $svc := $main.service -}}

--- # --------------------------------------------------------------------------------- NetworkPolicy
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "common.fullname" . }}-applicationsetcontroller
  labels:
    app.kubernetes.io/component: applicationset-controller
    {{- include "common.defaultLabels" . | nindent 4 }}
    {{- include "common.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $v.labels 
                          $main.labels ) ) | nindent 4 }}
  annotations:
    {{- include "common.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $v.annotations 
                          $main.annotations ) ) | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: applicationset-controller
      {{- include "common.defaultSelectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - port: {{ $svc.ports.webhook.port }}
          protocol: TCP
        - port: {{ $svc.ports.metrics.port }}
          protocol: TCP