{{- $v := .Values -}}
{{- $global := $v.global -}}
{{- $main := $v -}}
{{- $wl := $main.workload -}}
{{- $pod := $main.pod -}}

{{- if (eq (include "vaultwarden.workloadKind" ( dict "kind" $wl.kind )) "DaemonSet") -}}
--- # --------------------------------------------------------------------------------- DaemonSet
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "vaultwarden.fullname" . }}
  labels:
    {{- include "vaultwarden.defaultLabels" . | nindent 4 }}
    {{- include "vaultwarden.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $main.labels 
                          $wl.labels) ) | nindent 4 }}
  annotations:
    {{- include "vaultwarden.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $main.annotations 
                          $wl.annotations) ) | nindent 4 }}
spec:
  {{- with $wl.revisionHistoryLimit }}
  revisionHistoryLimit: {{ . }}
  {{- end }}
  {{- with $wl.minReadySeconds }}
  minReadySeconds: {{ . }}
  {{- end }}
  {{- with $wl.updateStrategy }}
  updateStrategy:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "vaultwarden.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- include "vaultwarden.keyValues" ( 
            dict "kvs" ( list $global.annotations 
                              $main.annotations 
                              $wl.annotations
                              $pod.annotations) ) | nindent 8 }}
      labels: 
        {{- include "vaultwarden.selectorLabels" . | nindent 8 }}
        {{- include "vaultwarden.keyValues" ( 
            dict "kvs" ( list $global.labels 
                              $main.labels 
                              $wl.labels
                              $pod.labels) ) | nindent 8 }}
    spec:
      {{- include "vaultwarden.pod" . | nindent 6 }}
{{- end -}}