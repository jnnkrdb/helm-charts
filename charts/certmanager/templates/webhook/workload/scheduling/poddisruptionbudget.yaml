{{- $v := .Values -}}
{{- $global := $v.global -}}
{{- $main := $v.controller -}}
{{- $pdb := $main.pdb -}}
{{- $wl := $main.workload -}}

{{- if not (eq (include "certmanager.workloadKind" ( dict "kind" $wl.kind )) "DaemonSet") -}}
{{- if $pdb.enabled -}}
--- # --------------------------------------------------------------------------------- PodDisruptionBudget
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "certmanager.fullname" . }}-controller
  labels:
    app: controller
    {{- include "certmanager.defaultLabels" . | nindent 4 }}
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $v.labels 
                          $main.labels 
                          $pdb.labels) ) | nindent 4 }}
  annotations:
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $v.annotations 
                          $main.annotations 
                          $pdb.annotations) ) | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: controller
      {{- include "certmanager.selectorLabels" . | nindent 6 }}
  {{- with $pdb.minAvailable }}
  minAvailable: {{ . }}
  {{- end }}
  {{- if not $pdb.minAvailable }}
  {{- with $pdb.maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- end }}
  {{- end }}
  {{- with $pdb.unhealthyPodEvictionPolicy }}
  unhealthyPodEvictionPolicy: {{ . }}
  {{- end }}
{{- end -}}
{{- end -}}