{{- $v := .Values -}}
{{- $global := $v.global -}}
{{- $main := $v.webhook -}}
{{- $wl := $main.workload -}}
{{- $pod := $main.pod -}}

{{- if (eq (include "certmanager.workloadKind" ( dict "kind" $wl.kind )) "StatefulSet") -}}
{{- if not $wl.serviceName -}}
--- # --------------------------------------------------------------------------------- Headless Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "certmanager.fullname" . }}-webhook-headless
  labels:
    app: webhook
    {{- include "certmanager.defaultLabels" . | nindent 4 }}
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $v.labels 
                          $main.labels ) ) | nindent 4 }}
  annotations:
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $v.annotations 
                          $main.annotations ) ) | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    {{- range $portName, $portSpecs := $svc.ports }}
    - name: {{ $portName | quote }}
      protocol: {{ $portSpecs.protocol }}
      targetPort: {{ $portSpecs.port }}
      port: {{ $portSpecs.port }}
    {{- end }}
  selector:
    app: webhook
    {{- include "certmanager.selectorLabels" . | nindent 4 }}
{{- end }}
--- # --------------------------------------------------------------------------------- Statefulset
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "certmanager.fullname" . }}-webhook
  labels:
    app: webhook
    {{- include "certmanager.defaultLabels" . | nindent 4 }}
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.labels 
                          $v.labels 
                          $main.labels 
                          $wl.labels) ) | nindent 4 }}
  annotations:
    {{- include "certmanager.keyValues" ( 
        dict "kvs" ( list $global.annotations 
                          $v.annotations 
                          $main.annotations 
                          $wl.annotations) ) | nindent 4 }}
spec:
  {{- if not $wl.serviceName }}
  serviceName: {{ include "certmanager.fullname" . }}-webhook-headless
  {{- else }}
  {{- with $wl.serviceName }}
  serviceName: {{ . | quote }}
  {{- end }}
  {{- end }}
  {{- with $wl.replicaCount }}
  replicas: {{ . }}
  {{- end }}
  {{- with $wl.revisionHistoryLimit }}
  revisionHistoryLimit: {{ . }}
  {{- end }}
  {{- with $wl.minReadySeconds }}
  minReadySeconds: {{ . }}
  {{- end }}
  {{- with $wl.podManagementPolicy }}
  podManagementPolicy: {{ . }}
  {{- end }}
  {{- with $wl.persistentVolumeClaimRetentionPolicy }}
  persistentVolumeClaimRetentionPolicy: 
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  {{- with $wl.volumeClaimTemplates }}
  volumeClaimTemplates:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  {{- with $wl.updateStrategy }}
  updateStrategy:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      app: webhook
      {{- include "certmanager.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- include "certmanager.keyValues" ( 
            dict "kvs" ( list $global.annotations 
                              $v.annotations 
                              $main.annotations 
                              $wl.annotations
                              $pod.annotations) ) | nindent 8 }}
      labels: 
        app: webhook
        {{- include "certmanager.selectorLabels" . | nindent 8 }}
        {{- include "certmanager.keyValues" ( 
            dict "kvs" ( list $global.labels 
                              $v.labels 
                              $main.labels 
                              $wl.labels
                              $pod.labels) ) | nindent 8 }}
    spec:
      {{- include "webhook.pod" . | nindent 6 }}
{{- end -}}